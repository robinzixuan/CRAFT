data:
  train_files: chuhac/R2D-R1@train
  val_files: NWULIST/R2D-R1-test@test
  prompt_key: input
  answer_key: output
  image_key: images
  video_key: videos
  image_dir: null
  video_fps: 2.0
  max_prompt_length: 1024
  max_response_length: 2048
  rollout_batch_size: 48  # must be divisible by actor.global_batch_size * rollout.n (16*3=48)
  mini_rollout_batch_size: null
  val_batch_size: 32
  format_prompt: ./examples/format_prompt/r1v.jinja
  override_chat_template: null
  shuffle: true
  seed: 238
  min_pixels: 262144
  max_pixels: 4194304
  filter_overlong_prompts: true

algorithm:
  adv_estimator: grpo
  disable_kl: false
  use_kl_loss: true
  kl_penalty: low_var_kl
  kl_coef: 1.0e-2
  online_filtering: false
  filter_key: overall
  filter_low: 0.01
  filter_high: 0.99

worker:
  actor:
    global_batch_size: 48  # rollout_batch_size * n = 16 * 3
    micro_batch_size_per_device_for_update: 4  # reduced: vocab 151k x 2048 seq x batch → OOM at 16
    micro_batch_size_per_device_for_experience: 4
    max_grad_norm: 1.0
    padding_free: false
    dynamic_batching: false
    ulysses_size: 1
    model:
      model_path: Qwen/Qwen3-0.6B
      enable_gradient_checkpointing: true  # 仍然开启，进一步节省显存
      trust_remote_code: false
      freeze_vision_tower: false
    optim:
      lr: 1.0e-6
      weight_decay: 1.0e-2
      strategy: adamw
      lr_warmup_ratio: 0.0
    fsdp:
      enable_full_shard: true
      enable_cpu_offload: false
      enable_rank0_init: true
    offload:
      offload_params: false
      offload_optimizer: false

  rollout:
    n: 3
    temperature: 0.6
    top_p: 0.95
    limit_images: 0
    gpu_memory_utilization: 0.4  # reduced: ~47 GB already used at baseline, leave room for actor FSDP
    enforce_eager: true
    enable_chunked_prefill: false
    tensor_parallel_size: 1
    disable_tqdm: false
    val_override_config:
      temperature: 0.6
      top_p: 0.95
      n: 1

  ref:
    fsdp:
      enable_full_shard: true
      enable_cpu_offload: false
      enable_rank0_init: true
    offload:
      offload_params: false

  reward:
    reward_type: batch
    reward_function: ./examples/reward_function/reasoning_trace.py:compute_score
    projection_ckpt_path: ./outputs/lclr/projection_head.pt
    safety_ckpt_path: ./outputs/lclr/safety_head.pt
    projection_dim: 512
    model_path: Qwen/Qwen3-0.6B
    num_gpus: 2  # no GPU needed: h_mean is computed by actor, reward only runs tiny CPU heads

trainer:
  total_epochs: 8
  max_steps: null
  project_name: easy_r1
  experiment_name: qwen3_0.6b_think_safety
  logger: ["file", "wandb"]
  nnodes: 1
  n_gpus_per_node: 2  # 2 张 H100
  max_try_make_batch: 20
  val_freq: -1
  val_before_train: false
  val_only: false
  val_generations_to_log: 3
  save_freq: 1
  save_limit: 6
  save_model_only: false
  save_checkpoint_path: ./checkpoints/qwen3_0_6b_craft
  load_checkpoint_path: null
  find_last_checkpoint: true
